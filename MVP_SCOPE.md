# 鲜管家 · MVP 范围文档（v2 — 功能大爆炸后重新研究）

> **文档用途**：给「编程对话」的施工图纸。v1 只覆盖"库存→配餐→花费"三件事；v2 纳入 SSS 提出的大批功能设想（图像识别、食材丰富属性、家庭画像、菜谱丰富属性、非食材冰箱物品），并逐条判定**进 MVP 还是后置**。
> **配套**：`index.html`（原型参考）、`BLUEPRINT.md`（蓝图）、`PROJECT_PLAN.md`（唯一路线图）、`RESEARCH.md`（故事圣经）。
> **状态**：v2 草稿，待 SSS review。

---

## 1. 重新研究的核心思路（判定原则）

你抛的功能把产品从"工具"升级成"家庭饮食操作系统"。但**一人公司 + MVP 要先跑通**，所以不能全塞进第一版。我用一条原则分类：

> **存储便宜、展示次之、复杂逻辑后置。**
> - **数据字段**：便宜，尽量进 MVP 数据模型（即使 UI 只展示一部分），避免以后重构。
> - **简单过滤**（忌口/厨具/预算/营养目标）：进 MVP，是"筛选"不是"智能"，成本低。
> - **复杂逻辑**（并行做菜排程、相克自动排除、替代方案、云端共建）：后置 v1.1 / 远期。

每个设想点给三档判定：**✅ 进 MVP** ／ **🟡 进 MVP 仅存数据/展示，逻辑后置** ／ **🔮 后置（v1.1 / 远期）**。

---

## 2. 你的 5 大设想 → 逐条判定

### 2.1 图像识别
- **设想**：拍照识别菜。
- **判定**：🔮 **MVP 核心仍用手动 + 条形码**；**v1.1 加"拍照识别"用多模态 LLM API**（拍照→发给 GPT-4V / 通义 VL / Qwen-VL 认出来，不训练模型，便宜）；🔮 远期再做本地/硬件识别（冰箱贴摄像头）。
- **理由**：自训识别模型贵且难；但多模态 LLM 当"识别器"是 2026 年现成的便宜解法，正好契合你"用 AI 工具"的打法。MVP 不卡在这。

### 2.2 食材丰富属性
| 属性 | 判定 | 说明 |
|------|------|------|
| 单位营养价值 | ✅ 进 MVP | 配餐算营养的核心，必做 |
| 素食 / 肉食 | ✅ 进 MVP | 标签，用于配餐均衡 + 过滤 |
| 疾病禁忌黑名单 | 🟡 存数据 + MVP 做**基础过滤** | 食材标"禁忌疾病"，用户家庭标"有某病"→配餐排除。成本低、价值高 |
| 忌口 / 个人口味 | ✅ 进 MVP | 忌口=黑名单过滤；口味=软加权 |
| 中医相生相克 | 🟡 存数据 + 🔮 v1.1 做"同餐相克提醒" | MVP 只存相克配对表，展示用；自动警示后置 |
| 单位价格 | ✅ 进 MVP | 核心 |
| 购买价 vs 平均价比较 | ✅ 进 MVP（展示） | 存 avgPrice，显示"你买贵/便宜了"——强省钱钩子 |
| 清洗方式 | 🟡 进 MVP（展示） | 存字段，食材详情里显示 |
| 保存方式 | 🟡 进 MVP（展示） | 同上 |
| 保质期 | ✅ 进 MVP | 核心 |
| 味道 | 🟡 进 MVP（标签） | 用于口味匹配 |
| 默认图片 | ✅ 进 MVP | UI 用 emoji/占位图，真图后续 |
| 速冻食物 / 预制菜 | ✅ 进 MVP（类别） | 不同保质期/行为，纳入类别体系 |

### 2.3 用户 / 家庭画像
| 属性 | 判定 | 说明 |
|------|------|------|
| 家庭人口数量 / 年龄 / 性别 | ✅ 进 MVP | 决定份量 + 营养目标基数 |
| 是否忌口 | ✅ 进 MVP | 黑名单过滤 |
| 是否生病 | 🟡 进 MVP（存 + 软建议） | 标"生病"→配餐软建议清淡；精准调整 v1.1 |
| 是否减脂 | ✅ 进 MVP | 营养目标：减脂/均衡/增肌 |
| 生理期 | 🟡 进 MVP（存 + 软建议） | 标"生理期"→建议补铁食物；精准调整 v1.1 |
| 个人口味 | ✅ 进 MVP | 软加权 |
| 家里厨具种类 | ✅ 进 MVP | 按厨具过滤菜谱（没烤箱就不推烤菜） |
| 预算 | ✅ 进 MVP | 核心 |

### 2.4 菜谱丰富属性
| 属性 | 判定 | 说明 |
|------|------|------|
| 需要的食材和数量 | ✅ 进 MVP | 核心 |
| 调料 | ✅ 进 MVP | 菜谱一部分 |
| 需要的厨具 | ✅ 进 MVP | 按用户厨具过滤 |
| 做菜时间 + **多菜同时做排程** | 🟡 存时间字段；🔮 排程优化 v1.1 | MVP 展示备菜/烹饪时长；"炖汤时炒别的"自动排程后置 |
| 备菜时间 / 提前多久备菜 | 🟡 进 MVP（存 + 展示） | 排程逻辑 v1.1 |
| 菜谱数据库云端共同建立 | 🔮 远期 | 需账号 + 云，放 v2 |
| 每餐价格 | ✅ 进 MVP（计算） | 核心 |
| 每餐营养价值 | ✅ 进 MVP（计算） | 核心 |
| 菜谱收藏 | ✅ 进 MVP（本地） | 便宜 |
| 菜谱录入（用户添加） | ✅ 进 MVP（简单版） | 用户填食材/厨具/时间即可；复杂校验 v1.1 |
| 缺少食材的替代方案 | 🔮 v1.1 | 需替代规则 / LLM 推断，后置 |

### 2.5 非食材类冰箱物品
- **设想**：药、剩菜、水果、酱料、即食、用户自定义也放冰箱。
- **判定**：✅ **进 MVP**——库存模型必须支持灵活 `kind` 类型 + `mealEligible` 标志。
  - **药**：`mealEligible=false`，有保质期/保存提示，绝不进配餐。
  - **剩菜**：`mealEligible=true` 且 `alreadyCooked=true`，短保期，配餐可建议"先吃剩菜"——强防浪费钩子。
  - **水果 / 即食**：可入餐或作加餐。
  - **酱料**：当调料类。
  - **用户自定义**：自由类别。
- **理由**：这是把"冰箱"真正管起来的关键，且只是数据模型加字段，成本低、差异化强（竞品多只管食材）。

---

## 3. 重新划定的 MVP 范围（v2）

**必做闭环（不变）**：入库 → 库存 → 配餐 → 花费。
**MVP 相对 v1 的新增**：
- 食材主数据含全部 2.2 属性字段（UI 展示核心子集）
- 家庭画像含人口/忌口/营养目标/厨具/预算 + 生病/生理期软建议
- 菜谱含厨具/时间/调料/收藏/用户录入
- 库存支持非食材类型（药/剩菜/水果/酱料/即食/自定义）
- 配餐时执行：忌口黑名单过滤、厨具过滤、预算约束、营养目标
- 价格对比展示（买贵/便宜）

**MVP 功能清单（页面级）**：
| 页面 | 功能（v2） |
|------|-----------|
| 库存 | 列表（按 kind 分类：食材/水果/酱料/剩菜/药/即食/自定义）；临期高亮；总花费；价格对比标；非食材类型正确显示且不进配餐 |
| 入库 | 扫条形码 / 手动选 / 🟡v1.1 拍照识别；填数量/单价/保质期；选 kind |
| 配餐 | 选日期+营养目标+预算；生成三餐（菜名+用库存+厨具+时长+每餐价+营养）；执行忌口/厨具/预算过滤；🟡软建议（生病清淡/生理期补铁）；🔮替代方案 v1.1 |
| 花费 | 今日/本周花费、营养达标、价格对比汇总 |
| 我的 | 家庭成员（人口/年龄/性别/忌口/病/生理期/减脂）、厨具清单、预算、营养目标默认 |

---

## 4. 不在 MVP（v2 明确后置）

- 🔮 自训图像识别模型 / 冰箱贴硬件（🔮 远期；v1.1 先用多模态 LLM 拍照识别）
- 🔮 多菜并行做菜排程优化（"炖汤时炒别的"自动时间表）
- 🔮 中医相克自动排除（v1.1 先做同餐提醒）
- 🔮 缺料自动替代方案
- 🔮 菜谱云端共建（需账号/云，v2）
- ❌ 电商分佣 / 付费 / 广告（战略不变）
- ❌ 多家庭账户、社交

---

## 5. 用户故事（扩展版精选）

- **US-1** 作为用户，我扫码/手动加食材并填价格保质期，APP 告诉我"这比平均价贵了"——强化省钱感知。
- **US-2** 作为用户，我加一盒药/一盘剩菜，它们出现在库存但不混入配餐（药），剩菜被建议优先吃。
- **US-3** 作为宝妈，我给家人标"痛风忌口/生理期/减脂"，配餐自动避开禁忌、软建议补铁/清淡。
- **US-4** 作为用户，我只有炒锅没烤箱，配餐不出需要烤箱的菜。
- **US-5** 作为用户，我点生成三餐，看到每餐食材/厨具/时长/价格/营养，钱和账一目了然。
- **US-6** 作为用户，我收藏好吃的菜谱、自己录一个家传菜谱。

---

## 6. 数据模型 v2（Expanded，给编程）

```
Ingredient（食材主数据，内置 JSON，v2 支持云端共建）
  id, name, emoji, defaultImage
  category(蔬菜/肉蛋/水产/主食/水果/乳制品/调料酱料/速冻/预制菜/其它)
  unit, nutritionPer100g{kcal,protein,fat,carb}   // 单位营养
  dietTag(veg/meat/omnivore)                       // 素食/肉食
  taste[]                                          // 味道标签
  contraindications[](疾病标签:痛风/糖尿病/高血压…)  // 疾病禁忌黑名单
  washMethod, storageMethod                        // 清洗/保存
  defaultShelfLifeDays, avgPrice                   // 保质期/平均价(对比用)
  isFrozen, isPrepared                             // 速冻/预制菜
  mealEligible=true

TcmConflict（中医相克配对表，独立表）
  a:ingredientId, b:ingredientId, note

InventoryItem（库存条目，用户数据）
  id, kind(ingredient/水果/酱料/剩菜/药/即食/自定义)
  ingredientId?, name, emoji, qty, unitPrice, totalPrice
  addedDate, expireDate
  mealEligible        // 由 kind 决定；药=false
  alreadyCooked?      // 剩菜=true（短保、建议优先吃）
  status(正常/临期/已坏)
  avgPrice?(冗余用于对比展示)

Recipe（菜谱）
  id, title, emoji
  ingredients[{ingredientId, qty, isOptional}]
  seasonings[]
  requiredCookware[]         // 过滤用
  prepTimeMin, cookTimeMin, leadTimeMin   // 备菜/烹饪/提前；排程 v1.1
  steps[](简)
  nutrition(per serving), price(computed)
  tags[], isFavorite, createdBy(system/user/cloud)

UserProfile
  householdSize
  members[{age, gender, dislikes[], diseases[], sick?, menstruation?, nutritionGoal?}]
  budget(daily), availableCookware[], nutritionGoalDefault

MealPlan
  date, meals[{type, title, usedItems[], requiredCookware[], prepTimeMin, cookTimeMin, cost, nutrition}]
  dailyCost, dailyNutrition, warnings[](忌口命中/相克提醒/软建议)
```

---

## 7. 技术路线（v2 更新）

| 维度 | 建议 | 变化 |
|------|------|------|
| 平台 | 微信小程序 | 不变 |
| 存储 | 本地（wx.storage） | 不变；v2 再加云同步 |
| AI 配餐 | LLM API（DeepSeek/通义）prompt 工程 | 不变；prompt 需注入家庭画像+忌口+厨具+预算 |
| **图像识别** | **v1.1 接多模态 LLM（GPT-4V/通义VL/Qwen-VL）拍照识别**，不训练 | 🆕 比 v1 新增路径 |
| 价格 | 手动 + 常见价写死 + avgPrice 对比 | 不变 |
| 条形码 | wx.scanCode + 公开条码库 | 不变 |
| 营养 | 内置常见食材营养表 | 不变，字段已扩 |
| 菜谱 | 内置基础库 + 用户录入 | 不变；云端共建 v2 |

**兜底**：LLM/多模态调用失败 → 本地规则降级，不白屏。

---

## 8. 边界规则 v2

- 临期阈值 ≤2 天高亮（可调）
- 药 / 即食 / 自定义：`mealEligible` 控制是否进配餐
- 剩菜：`alreadyCooked`，配餐优先建议吃，短保
- 配餐后 MVP **不自动扣库存**（只建议）——见 Q3
- 同餐相克：v1.1 提醒；MVP 仅存表
- 生病/生理期：MVP 软建议，非硬排除

---

## 9. 开放问题（v2，等你拍板）

| # | 问题 | 我的建议 |
|---|------|---------|
| Q1 | 平台：微信小程序 / 网页 PWA | 微信小程序 |
| Q2 | 营养目标预设 | 均衡/减脂/增肌，默认均衡 |
| Q3 | 配餐后自动扣库存？ | **不扣**，只建议 |
| Q4 | 条码查不到 | 转手动填 |
| Q5 | 第一版做单日还是周计划 | 单日，周计划 v1.1 |
| **Q6** | **v1.1 要不要先做"拍照识别（多模态LLM）"还是"并行排程"？** | 建议先拍照识别（更直观、涨粉素材好） |
| **Q7** | **中医相克提醒 v1.1 做不做？** | 做轻量提醒（存表+同餐警示），不做硬排除 |
| **Q8** | **剩菜"建议优先吃"要做到什么程度？** | MVP 仅提示；自动排进配餐 v1.1 |

---

## 10. 与路线图对应

- 本文档 = `PROJECT_PLAN.md` 阶段 2（MVP 范围）的详细版，v2。
- 阶段 1（开源门面）✅；阶段 3（MVP 实现）按本文档搭；v1.1 增量见 §4 后置项。

---

*由设计/原型对话产出，待 SSS review。调整直接说，我改完同步 GitHub。v1→v2 变化：纳入 5 大功能设想，确立"存数据进 MVP、复杂逻辑后置"原则。*
