# 鲜管家 · MVP 范围文档（v3 — Q6/Q7/Q8 拍板 + 补充 6 设想）

> **文档用途**：给「编程对话」的施工图纸。v2 纳入 5 大功能设想（图像识别 / 食材丰富属性 / 家庭画像 / 菜谱丰富属性 / 非食材库存）。v3 在 Q6/Q7/Q8 拍板后，补充 SSS 第十轮讨论的 6 点（平台 / 社区 / 扩展性 / 易用性 / 广告+VIP / 学做菜），并同步修正商业模式判定。
> **配套**：`index.html`（原型参考）、`BLUEPRINT.md`（蓝图）、`PROJECT_PLAN.md`（唯一路线图）、`RESEARCH.md`（故事圣经）。
> **状态**：v3 草稿，待 SSS review。

---

## 1. 判定原则（三条）

你抛的功能把产品从"工具"升级成"家庭饮食操作系统"。但**一人公司 + MVP 要先跑通**，用三条原则分类：

> **原则 A · 存储便宜、展示次之、复杂逻辑后置**
> - **数据字段**：便宜，尽量进 MVP 数据模型（即使 UI 只展示一部分），避免以后重构。
> - **简单过滤**（忌口/厨具/预算/营养目标）：进 MVP，是"筛选"不是"智能"，成本低。
> - **复杂逻辑**（并行做菜排程、相克硬排除、替代方案、云端共建）：后置 v1.1 / v2 / 远期。

> **原则 B · 易用性最高优先（第十轮新增，总原则）**
> - 用户**不碰任何公式/参数**：配餐一键生成，营养求和、花费汇总、排程全在背后跑。
> - **藏复杂、秀亮点**：枯燥算式藏起来；但"复杂计算得出的有意思结果"要展示出来当爽点——
>   如「本周省 ¥23.5、少浪费 1.2kg、营养达标 92%」「先炖汤再炒菜最省时」做成成就/提示页。
> - 这条原则直接影���原型加屏（MVP 真做出来后，本对话据此补屏）。

> **原则 C · 功能可扩展（第十轮新增，架构原则）**
> - 现在设想的功能可能不够。数据模型与功能开关要留**扩展位**（标签 / 自定义类别 / 插件式），新增功能不推翻重构。

> **原则 D · LLM 中枢 + 可插拔推理（第十一论新增，架构原则）**
> - 整个 App 的"智能"由 **LLM 驱动**，不是写死的规则：配餐、营养搭配建议、缺料替代、拍照识别都走推理层（prompt 工程 + 工具调用）。
> - **推理层可插拔**：默认走云端 LLM API（DeepSeek / 通义 / GLM，便宜够用）；同时预留**本地小模型**通道（隐私优先 / 离线可用），契合"开源 + 隐私信任"定位。
> - 兜底：LLM / 多模态调用失败 → 本地规则降级，不白屏（已有）。

每个设想点给三档判定：**✅ 进 MVP** ／ **🟡 进 MVP 仅存数据/展示，逻辑后置** ／ **🔮 后置（v1.1 / v2 / 远期）**。

---

## 2. 你的 5 大设想 → 逐条判定（v2 已定，v3 无变化）

### 2.1 图像识别
- **判定**：🔮 **MVP 核心仍用手动 + 条形码**；**v1.1 加"拍照识别"用多模态 LLM API**（GPT-4V / 通义 VL / Qwen-VL，不训练模型，便宜）；🔮 远期自训 / 冰箱贴硬件。
- **★ Q6 已拍板**：v1.1 **先做人做"拍照识别"**（比并行排程更直观、涨粉素材更好）。

### 2.2 食材丰富属性
| 属性 | 判定 | 说明 |
|------|------|------|
| 单位营养价值 | ✅ 进 MVP | 配餐算营养核心 |
| 素食 / 肉食 | ✅ 进 MVP | 标签，均衡 + 过滤 |
| 疾病禁忌黑名单 | 🟡 存数据 + MVP 基础过滤 | 食材标禁忌疾病，家庭标有某病→配餐排除 |
| 忌口 / 个人口味 | ✅ 进 MVP | 忌口=黑名单过滤；口味=软加权 |
| 中医相生相克 | 🟡 存数据 + 🔮 v1.1 同餐提醒 | **★ Q7 已拍板**：做轻量提醒（存表+同餐警示），不硬排除 |
| 单位价格 | ✅ 进 MVP | 核心 |
| 购买价 vs 平均价 | ✅ 进 MVP（展示） | "你买贵/便宜了"——强省钱钩子 |
| 清洗 / 保存方式 | 🟡 进 MVP（展示） | 食材详情显示 |
| 保质期 | ✅ 进 MVP | 核心 |
| 味道 | 🟡 进 MVP（标签） | 口味匹配 |
| 默认图片 | ✅ 进 MVP | emoji/占位图 |
| 速冻 / 预制菜 | ✅ 进 MVP（类别） | 不同保质期/行为 |

### 2.3 用户 / 家庭画像
| 属性 | 判定 | 说明 |
|------|------|------|
| 人口 / 年龄 / 性别 | ✅ 进 MVP | 份量 + 营养基数 |
| 忌口 | ✅ 进 MVP | 黑名单过滤 |
| 生病 | 🟡 存 + 软建议 | 标"生病"→配餐软建议清淡；精准调整 v1.1 |
| 减脂 | ✅ 进 MVP | 营养目标：减脂/均衡/增肌 |
| 生理期 | 🟡 存 + 软建议 | 标"生理期"→建议补铁；精准调整 v1.1 |
| 口味 | ✅ 进 MVP | 软加权 |
| 厨具 | ✅ 进 MVP | 按厨具过滤菜谱 |
| 预算 | ✅ 进 MVP | 核心 |

### 2.4 菜谱丰富属性
| 属性 | 判定 | 说明 |
|------|------|------|
| 食材和数量 | ✅ 进 MVP | 核心 |
| 调料 | ✅ 进 MVP | 菜谱一部分 |
| 厨具 | ✅ 进 MVP | 按用户厨具过滤 |
| 做菜时间 + 多菜并行排程 | 🟡 存时间字段；🔮 排程 v1.1 | MVP 展示时长；自动排程后置 |
| 备菜 / 提前量 | 🟡 进 MVP（存+展示） | 排程逻辑 v1.1 |
| 云端共建 | 🔮 v2 | 需账号+云 |
| 每餐价格 / 营养 | ✅ 进 MVP（计算） | 核心 |
| 收藏 | ✅ 进 MVP（本地） | 便宜 |
| 用户录入 | ✅ 进 MVP（简单版） | 填食材/厨具/时间即可 |
| 缺料替代 | 🔮 v1.1 | LLM 推断 |

### 2.5 非食材类冰箱物品
- **判定**：✅ **进 MVP**——库存模型支持灵活 `kind` + `mealEligible`。
  - 药：`mealEligible=false`，有保质期/保存提示，绝不进配餐。
  - 剩菜：`mealEligible=true` 且 `alreadyCooked=true`，短保；**★ Q8 已拍板**：MVP 仅提示"先吃剩菜"，v1.1 自动排进配餐。
  - 水果 / 即食：可入餐或加餐。酱料：当调料。自定义：自由类别。

---

## 3. 补充设想（第十轮讨论，2026-07-31 凌晨）★

SSS 在 Q6/Q7/Q8 拍板后继续补充 6 点，逐条判定与落地方向：

| # | 设想 | 判定 | 说明 / 落地方向 |
|---|------|------|----------------|
| 1 | 平台：小程序测试用，未来做原生 APP | ✅ 决策 | **MVP=微信小程序**；原生 APP 放 **v2**（小程序验证后再做），不进 MVP 范围变化 |
| 2 | 用户社区（类似爱厨房） | 🔮 v2 | 需账号+云+审核；MVP 无社交。社区放有用户量后，承接 UGC 菜谱与内容飞轮 |
| 3 | 功能可扩展 | ✅ 架构原则 | 数据模型留扩展位（标签/自定义类别/插件式开关）；新增功能不推翻重构。见 §7 |
| 4 | 易用性最高（傻瓜式） | ✅ 总原则 | 见 §1 原则 B：藏复杂算、秀亮点结果。直接影响原型加屏 |
| 5 | 广告 + VIP（免广告+其他功能） | 🔮 v2 | **MVP 无广告保持干净**；v2 引入轻度广告 + VIP（免广告 + 增值：云同步/更聪明配餐/多家庭）。**VIP 不锁基础功能**，与开源免费不冲突 |
| 6 | 学做菜 | 🟡 菜谱步骤 MVP 已有；🔮 v1.1 加引导式 | MVP 菜谱含 `steps[]` 可看；**v1.1** 加"分步计时/视频"引导模式 |
| 7 | **支持 LLM / 本地小模型** | ✅ 架构原则；🟡 MVP 云端 LLM 中枢；🔮 v1.1 加本地小模型 + 可切换厂商 | App 的"大脑"是 LLM（配餐/替代/建议/识别全走推理层）；推理层可插拔：默认云端 API，可选**本地小模型**（隐私/离线）。与开源信任定位一致；直接支撑"藏复杂算、秀亮点"的易用性原则 |

> ⚠️ **商业模式反转**：第 5 点推翻了早期"广告 ❌（伤体验/隐私）"的判定。SSS 主动要求"支持广告 + VIP 免广告"，故改为 ✅（v2，带 VIP 去广告）。开源免费仍成立（代码公开+基础免费），VIP 是增值非锁基础。详见 RESEARCH §1 第十幕。

---

## 4. 重新划定的 MVP 范围（v3）

**必做闭环**：入库 → 库存 → 配餐 → 花费（不变）。
**MVP 相对 v2 的新增**：仅第 3 点的「扩展性架构原则」与第 4 点的「易用性原则」落地到设计与数据模型；功能清单本身与 v2 一致（学做菜的 `steps[]` 本就在 MVP）。

**MVP 功能清单（页面级）**：
| 页面 | 功能（v3） |
|------|-----------|
| 库存 | 列表（按 kind 分类）；临期高亮；总花费；价格对比标；非食材正确显示且不进配餐 |
| 入库 | 扫条形码 / 手动选 / 🟡v1.1 拍照识别；填数量/单价/保质期；选 kind |
| 配餐 | 选日期+营养目标+预算；生成三餐（菜名+用库存+厨具+时长+每餐价+营养）；忌口/厨具/预算过滤；🟡软建议；🔮替代方案 v1.1；**易用性：一键生成、复杂算藏在背后** |
| 花费 | 今日/本周花费、营养达标、价格对比汇总；**易用性：把"省了多少/营养达标率"做成成就展示（秀亮点）** |
| 我的 | 家庭成员画像、厨具清单、预算、营养目标默认 |

---

## 5. 不在 MVP（v3 明确后置）

- 🔮 自训图像识别 / 冰箱贴硬件（远期；v1.1 先多模态 LLM 拍照）
- 🔮 多菜并行做菜排程（v1.1）
- 🔮 中医相克硬排除（v1.1 先做同餐提醒）
- 🔮 缺料自动替代（v1.1）
- 🔮 剩菜自动排餐（v1.1，MVP 仅提示）
- 🔮 引导式学做菜（v1.1）
- 🔮 用户社区（v2，需账号/云）
- 🔮 广告 + VIP（v2，MVP 无广告）
- 🔮 原生 APP（v2）
- 🔮 菜谱云端共建（v2）

---

## 6. 用户故事（精选）

- **US-1** 扫码/手动加食材填价，APP 告诉我"比平均价贵了"——强化省钱感知。
- **US-2** 加药/剩菜，药不混配餐，剩菜被建议优先吃。
- **US-3** 宝妈给家人标"痛风/生理期/减脂"，配餐自动避禁忌、软建议补铁/清淡。
- **US-4** 只有炒锅没烤箱，配餐不出需烤箱的菜。
- **US-5** 点生成三餐，每餐食材/厨具/时长/价/营养一目了然（复杂算藏在背后）。
- **US-6** 收藏好吃的菜、自己录家传菜谱。
- **US-7（易用性）** 我不用懂营养学，APP 把"本周省 ¥23.5、营养达标 92%"当成成就秀给我看。

---

## 7. 数据模型 v3（扩展架构）

```
Ingredient（食材主数据）
  id, name, emoji, defaultImage
  category(蔬菜/肉蛋/水产/主食/水果/乳制品/调料酱料/速冻/预制菜/其它)
  unit, nutritionPer100g{kcal,protein,fat,carb}
  dietTag(veg/meat/omnivore)
  taste[]                         // 味道标签（扩展位）
  contraindications[]             // 疾病禁忌黑名单
  washMethod, storageMethod
  defaultShelfLifeDays, avgPrice
  isFrozen, isPrepared
  tags[](扩展位：未来新属性走标签，不新增字段)   // ★ 原则 C 扩展性
  mealEligible=true

TcmConflict（中医相克配对表）
  a:ingredientId, b:ingredientId, note

InventoryItem（库存条目，用户数据）
  id, kind(ingredient/水果/酱料/剩菜/药/即食/自定义)  // kind 可扩展
  ingredientId?, name, emoji, qty, unitPrice, totalPrice
  addedDate, expireDate
  mealEligible          // 由 kind 决定；药=false
  alreadyCooked?        // 剩菜=true
  status(正常/临期/已坏)
  avgPrice?

Recipe（菜谱）
  id, title, emoji
  ingredients[{ingredientId, qty, isOptional}]
  seasonings[]
  requiredCookware[]
  prepTimeMin, cookTimeMin, leadTimeMin
  steps[](含可选 timerSec/图文，支撑 v1.1 引导式学做菜)
  nutrition(per serving), price(computed)
  tags[], isFavorite, createdBy(system/user/cloud)
  extensible: pluginFlags{}   // ★ 原则 C：功能开关，新增功能不重构

UserProfile
  householdSize
  members[{age, gender, dislikes[], diseases[], sick?, menstruation?, nutritionGoal?}]
  budget(daily), availableCookware[], nutritionGoalDefault

ModelConfig（推理层配置，可插拔 ★ 原则 D）
  mode(cloud/local)
  cloudProvider?(DeepSeek/通义/GLM...)   // MVP 默认云端
  apiEndpoint?
  localModel?(on-device 端侧小模型标识)     // v1.1 本地小模型：★已定方案=手机端侧跑小模型（隐私最强/零服务器成本），非自托管端点
  preferLocalForPrivacy?                  // 隐私优先走本地

MealPlan
  date, meals[{type, title, usedItems[], requiredCookware[], prepTimeMin, cookTimeMin, cost, nutrition}]
  dailyCost, dailyNutrition, warnings[](忌口命中/相克提醒/软建议)
```

---

## 8. 技术路线（v3 更新）

| 维度 | 建议 | 变化 |
|------|------|------|
| **平台** | **MVP=微信小程序**；原生 APP 放 v2 | 🆕 第十轮明确：小程序先测，APP 后做 |
| 存储 | 本地（wx.storage）；v2 云同步 | 不变 |
| AI 配餐 | LLM API（DeepSeek/通义）prompt 工程 | 不变 |
| 图像识别 | v1.1 多模态 LLM 拍照识别 | 不变（Q6 先做人做） |
| 扩展性架构 | 标签/自定义类别/插件开关 | 🆕 原则 C |
| 易用性 | 一键生成 + 背后算 + 成就页秀亮点 | 🆕 原则 B |
| 广告+VIP | v2 轻度广告 + VIP（免广告+增值） | 🆕 第十轮反转 |
| 社区 | v2（账号+云） | 🆕 第十轮 |
| AI 中枢 / 推理层 | LLM 驱动（配餐/替代/建议/识别）；推理层可插拔：默认云端 API，预留本地小模型通道 | 🆕 第十一论：明确"LLM 中枢 + 本地小模型选项" |
| 价格/条形码/营养 | 手动+写死+扫码+内置表 | 不变 |

**兜底**：LLM/多模态失败 → 本地规则降级，不白屏。

---

## 9. 边界规则 v3

- 临期阈值 ≤2 天高亮（可调）
- 药/即食/自定义：`mealEligible` 控制是否进配餐
- 剩菜：`alreadyCooked`，MVP 仅提示优先吃；v1.1 自动排餐
- 配餐后 MVP **不自动扣库存**（只建议）——见 Q3
- 同餐相克：v1.1 提醒；MVP 仅存表
- 生病/生理期：MVP 软建议，非硬排除
- **易用性**：所有计算默认隐藏；仅在"成就/省钱/营养达标"页展示亮点结果

---

## 10. 开放问题（v3 状态）

| # | 问题 | 状态 |
|---|------|------|
| Q1 | 平台：小程序 / PWA | ✅ 微信小程序 |
| Q2 | 营养目标预设 | 均衡/减脂/增肌，默认均衡 |
| Q3 | 配餐后自动扣库存？ | **不扣**，只建议 |
| Q4 | 条码查不到 | 转手动填 |
| Q5 | 单日 / 周计划 | 单日，周计划 v1.1 |
| Q6 | v1.1 先做拍照识别还是排程 | ✅ **先拍照识别** |
| Q7 | 中医相克提醒 v1.1 做不做 | ✅ **做轻量提醒** |
| Q8 | 剩菜优先吃程度 | ✅ **MVP 提示；v1.1 自动排餐** |
| Q9 | 社区时机 | 建议 v2（需云账号，先不做） |
| Q10 | 广告+VIP 时机 | 建议 v2（先有用户量） |
| Q11 | 原生 APP 时机 | 建议 v2（小程序验证后） |
| Q12 | 本地小模型实现方案 | ✅ MVP 先云端 LLM + 规则兜底；🔮 v1.1 加本地小模型通道。**方案选定：手机端侧 on-device 小模型（非自托管端点）**；仍保留可切换云端厂商(DeepSeek/通义/GLM) |

---

## 11. 与路线图对应

- 本文档 = `PROJECT_PLAN.md` 阶段 2（MVP 范围）详细版，v3。
- 阶段 3（MVP 实现）按本文档搭；v1.1 增量见 §5；v2 商业化/社区/APP 见路线图。
- **原型加屏（设计对话）**：MVP 真做出来后，本对话据「原则 B 易用性 + 第 3 节补充设想 + 原则 D」补屏：拍照识别页 / 成就省钱页 / 学做菜引导页 / 社区页 / VIP 页 / **模型设置页（选云端 LLM / 本地小模型，隐私优先开关）**。

---

*由设计/原型对话产出，待 SSS review。v2→v3 变化：Q6/Q7/Q8 拍板；补充 6 设想（平台/社区/扩展/易用/广告+VIP/学做菜）；商业模式反转（广告从 ❌ 改 ✅ v2）；数据模型加扩展性位。v3 + 第十一论补充：明确 App 智能由 LLM 驱动、推理层可插拔（默认云端、可选本地小模型），本地小模型置 v1.1；新增原则 D + 数据模型 ModelConfig + Q12。Q12 后续拍板：本地小模型方案选定**手机端侧 on-device 小模型**（非自托管端点）。*
